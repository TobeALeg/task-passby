# Worket v0.1.2 发布验收

发布时间：2026-09-11 17:25（Asia/Shanghai）。

- 正式 Release：https://github.com/TobeALeg/worket/releases/tag/v0.1.2
- 发布标签：`v0.1.2`，构建提交：`7d8c28243b29376790c9ceae51ea2c86a44e67b6`。
- GitHub 已确认 `draft=false`、`prerelease=false`，`releases/latest` 指向本版本。
- 发布重点：优化界面显示、任务断点持续记录、边框吸附。

## 最终附件

`Worket-0.1.2-darwin-arm64.zip`：132015509 字节。

SHA256：`7dde9272fc855a069dda567631d3b5c4dc99e6accd7de66e78f22b3f5ab1db05`。

随附 `Worket-0.1.2-darwin-arm64.zip.sha256`；两份附件均为 uploaded，GitHub 返回的 digest 与本机文件一致。安装包采用 ad-hoc 签名，未经过 Apple 公证。

## 验证

- `npm run release:mac`：116 项测试全部通过；打包、签名、最终 ZIP 解压后的严格签名验证、原生窗口与 Dock、桌宠阴影、更新菜单及下载校验流程通过。
- 最终 ZIP 跨版本检查：使用 v0.1.1 创建合成记录，再由 v0.1.2 打开同一测试数据目录；工作 ID 与活动绑定保留，旧档案缺失的状态更新得到补处理，最近回复与处理游标正确。
- 公开更新检查：已发布的 v0.1.1 应用通过自己的更新读取器请求公开 GitHub API，成功发现 v0.1.2 和 ZIP / SHA256 两份附件。
- 桌宠拖动与边缘收纳原生回归通过；为满足原有阴影门槛增加 1px 透明空间，并修正跨工作区窗口设置后 Dock 的初始化顺序。
- 本机恢复运行同一 v0.1.2 签名应用，用户数据目录保持不变。

合成更新测试拦截了系统确认按钮和 Finder 显示，没有自动替用户替换应用；公开更新检查验证了新版发现及附件识别，未代替用户完整的手动替换操作。未发布或部署后台服务。
